{{#if ACC001IsPermitted}}
  {{#if isEditing}}
  <div>
      <!--<h4>Add Code for {{flag}} </h4>-->
      <form>
          <!--<div class="row">
              <div class="large-12 columns">
                  <label>Tile
                    {{input value=title  id="title" type="text" }}
                  </label>
              </div>
          </div>-->
          <br>
          <div class="form-group">
              <table style="width:20%">
                <tr>
                  <td>Code:</td>
                  <td>{{input value=code  id="code" type="text" }}</td> 
                </tr><br>
                <tr>
                  <td>Number:</td>
                  <td>{{input value=number  id="number" type="number" }}</td> 
                </tr><br>
                <tr>
                  <td>Name:</td>
                  <td>{{input value=name  id="name" type="text" }}</td> 
                </tr><br>
                <tr>
                  <td>Unit:</td>
                  <td>{{input value=unit  id="unit" type="text" }}</td> 
                </tr><br>
              </table>
              
          </div>
      </form>
      <br>
      <div>
          <a href="#" class="btn btn-default" role="button" {{action 'savePost'}}>Save</a>
          <a href="#" class="btn btn-default" role="button" {{action 'cancel'}}>Cancel</a>
      </div>
  </div>
  
  
  {{else}}
      <a href="#" class="btn btn-default" role="button" {{action 'addNewCode'}}>Add Code</a>
  {{/if}}
{{else}}
{{/if}}

